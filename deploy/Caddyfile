{
	email {$ACME_EMAIL}
	# Staging avoids the 5-per-week rate limit while testing DNS.
	# acme_ca https://acme-staging-v02.api.letsencrypt.org/directory
}

(common) {
	header {
		-Server
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		X-Frame-Options "SAMEORIGIN"
	}
	encode zstd gzip
}

aysanz.dev {
	import common
	root * /srv/portfolio
	try_files {path} {path}/ /index.html
	file_server

	@hashed path /assets/* /_next/static/* /static/*
	header @hashed Cache-Control "public, max-age=31536000, immutable"
	header /index.html Cache-Control "no-cache"
}

www.aysanz.dev {
	redir https://aysanz.dev{uri} permanent
}

alidade.aysanz.dev {
	import common
	reverse_proxy web:80 {
		header_up X-Forwarded-Proto {scheme}
	}
}

# Next project: add its container to the edge network and copy this block.
#
# thing.aysanz.dev {
# 	import common
# 	reverse_proxy thing-web:80
# }
